const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../categories';

describe('categories.actions.categories', () => {
  it('should do something');
});
