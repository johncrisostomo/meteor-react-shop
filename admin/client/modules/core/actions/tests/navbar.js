const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../navbar';

describe('core.actions.navbar', () => {
  it('should do something');
});
